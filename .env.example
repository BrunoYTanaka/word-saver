# Copy to .env.local to override. Requires a rebuild to take effect.

# Set to "false" to disable the "Importar de Foto" crossword-clue OCR import
# feature (client-side Tesseract.js). Enabled by default.
VITE_ENABLE_CROSSWORD_IMPORT=true
